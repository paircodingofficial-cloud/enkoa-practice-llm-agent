# LangChain 에이전트와 도구 — 실습 진행 가이드

이 체크리스트를 위에서부터 따라가면 오늘 실습을 빠짐없이 완주할 수 있습니다.
막히면 각 항목의 **힌트**와 **정답 노트북**을 참고하세요.

> **본인 `OPENAI_API_KEY` 가 필요합니다.** 지난 단원들에서 쓰던 키를 그대로 쓰면 됩니다 — 오늘 에이전트는 실제로
> 모델을 부릅니다. `.env.example` 을 `.env` 로 복사해 키를 채우세요.
> **데이터베이스는 준비물이 없습니다** — SQL 단원에서 배운 `sqlite` 를 쓰므로 접속 정보가 필요 없습니다.
>
> 모델이 만든 답과 **어떤 도구를 몇 번 부를지는 실행할 때마다 달라집니다.** 그래서 자가채점은 문장이나 값이 아니라
> **타입·구조**(도구가 최소 1회 불렸는지, 스키마가 허용한 값인지, 표의 열이 맞는지)로 확인합니다.
> 다만 **모델이 지어내면 안 되는 값**(도구가 계산한 값·데이터베이스에서 조회한 값·집계 결과)은 정확히 대조합니다.
>
> **검색 평가(평가셋·네 지표·`k` 고르기)는 다음 단원(day20)** 에서 다룹니다 — 교안_03 과 `과제_LV1_RAG평가.ipynb`.

## 0. 준비

- [ ] **필요한 라이브러리를 설치**하세요 — 터미널에서 아래를 실행합니다(환경 구축도 실습의 일부입니다).

```bash
uv add jupyter langchain langchain-core langchain-openai langchain-chroma langchain-huggingface langchain-text-splitters chromadb sentence-transformers requests pydantic pandas python-dotenv   # day19 (jupyter 안에 VS Code 커널용 ipykernel 포함)
```

- [ ] VS Code 로 노트북을 열고 커널은 **Python 3 (ipykernel)** 을 고른다.
- [ ] `.env.example` 을 `.env` 로 복사하고 본인 `OPENAI_API_KEY` 를 채운다.
- [ ] 각 노트북 맨 위 **준비 셀(`# [제공 코드]`)** 을 먼저 실행한다. 임베딩 모델은 처음 불러올 때 잠시 걸립니다.

## 1. 교안_01 — 구조화된 출력과 도구

- [ ] **1. 구조화된 출력** — 지난 단원에서 손으로 하던 **정형화를 LangChain 부품 한 줄**로. 스키마 규칙(`Literal`·`Optional`·`description`)은 **복습으로 짚고** 넘어가고, 새로 외울 것은 `model.with_structured_output(스키마).invoke(문장)` 하나다. 결과가 **문자열이 아니라 객체**로 오는 것, 분류기를 **한 번 만들어 여러 문의에 재사용**하는 것, 구조화된 출력을 배운 단원의 `parse` 와의 대응(**모델 회사를 바꿔도 스키마·호출은 그대로**). 따라하기(회의실 예약 문의)·퀴즈.
- [ ] **2. 도구 만들기(`@tool`)** — docstring 이 명세, 타입힌트가 인자 타입. `.name`·`.description`·`.args`, 모델에게 넘어가는 **JSON 명세**(`tool_call_schema.model_json_schema()`), 도구를 직접 불러 보기(아직 모델은 등장하지 않는다). **⚖️ 구조화된 출력 vs 도구** 갈림길("이 답을 만들려면 우리 데이터를 뒤져야 하나?"). 따라하기(구내식당 메뉴 도구)·퀴즈.
- [ ] **3. `create_agent` 와 메시지 궤적** — `agent.invoke({'messages': '질문'})`, 사람→AI(도구호출)→ToolMessage→AI(최종답). `system_prompt` 로 역할 부여, 도구가 필요 없는 질문(호출 0회), `response_format` 으로 **도구도 쓰고 답도 스키마로**. 따라하기(회의실 좌석 도구)·퀴즈.
- [ ] **4. 의미 있는 도구 만들기** — **실패는 문자열로 돌려준다**(예외를 던지면 에이전트가 그 자리에서 멈춘다), **할 수 있는 일을 좁게**(읽기 전용·상한을 도구 안에). 실무형 도구 3종(사내 데이터 조회·규칙 계산·**외부 REST API** — `try`/`except` 로 실패도 문자열로). 따라하기(주차 등록 도구)·퀴즈.
  - *도구가 여럿일 때 생기는 문제(설명이 선택을 좌우한다·인자를 다듬는다)는 **다음 단원**에서 증상과 함께 다룹니다.*

## 2. 교안_02 — RAG 파이프라인·Text-to-SQL

- [ ] **1. RAG 를 LangChain 부품으로** — 앞선 RAG 단원에서 손으로 만든 것을 부품으로 다시. `Document`(page_content·metadata) → `RecursiveCharacterTextSplitter`(metadata 가 조각에 따라붙는다) → `HuggingFaceEmbeddings`(768차원) → `Chroma.from_documents`(**`ids` 를 넘겨 여러 번 실행해도 중복되지 않게**) → `as_retriever(search_kwargs={'k': 2})`. 따라하기(서점 FAQ)·퀴즈.
- [ ] **2. RAG 체인(LCEL)** — `{'context': retriever | format_docs, 'question': RunnablePassthrough()} | prompt | model | parser`. "자료에 있는 내용만으로 답하라"가 왜 필요한가. `RunnableParallel` 로 **근거를 함께** 돌려받기(출처 없는 RAG 답은 실무에서 못 쓴다). 따라하기(서점 안내 체인)·퀴즈.
  - 절 끝에 **미결 하나**를 남깁니다 — 인사말을 넣어도 검색이 도는 것을 눈으로 보고, **다음 단원**에서 해결합니다.
  - 그 검색이 **얼마나 맞히고 있는지** 재는 법도 다음 단원(day20 교안_03)에서 다룹니다.
- [ ] **3. Text-to-SQL** — 표 구조를 프롬프트로 알려 주면 모델이 SQL 을 만든다(**모델은 DB 를 못 본다 — 스키마 설명이 모델의 눈**). 데이터 살펴보기 → **두 겹 가드**: 1) `run_select` 의 문자열 검사(SELECT 한 문장만) 2) **읽기 전용 연결**. `union` 을 섞은 조회처럼 **문자열 검사를 통과하는 SELECT** 를 직접 실행해 "문자열 검사만으론 못 막는다"를 확인하고, 읽기 전용 연결에 쓰기를 시도해 막히는 것도 확인. 궤적에서 **모델이 만든 SQL** 을 꺼내 보기. **구조화된 출력으로 SQL+근거+사용한 표** 받기. 따라하기(서점 DB)·퀴즈.

## 3. 과제 LV1 (기초) — 도구 정의·명세·에이전트 궤적 (8문)

데이터/도구: 준비 셀에서 제공. 도구를 **만드는** 문제는 직접 호출로(결정적), **에이전트 궤적** 문제는 실호출이라 도구가 최소 1회 불렸는지로 채점.

- [ ] **1** 할인가 계산 도구(`discount_price`) · **2** 제목 정리 도구(`clean_title`)
- [ ] **3** 도구 명세 읽기(`tool_name`·`tool_desc`·`arg_names`)
- [ ] **4** 배송비 도구 궤적(`res4`·`tool_msgs4`)
- [ ] **5** 도구가 필요 없는 질문(`res5`·`n_tools5`=0)
- [ ] **6** 역할 부여(정중, `res6`·`tool_msgs6`)
- [ ] **7** 구조화 출력 — `with_structured_output` 부품 재사용(`reviewer` 하나로 리뷰 두 건 → `summary_a`·`summary_b`; 스키마는 제공, 채점은 타입·방향)
- [ ] **8** 메시지 타입 개수·서술(`n_human`·`n_tool`·`n_ai`)

## 4. 과제 LV2 (응용) — RAG 체인·SQL·외부 API (3문)

- [ ] 준비 셀 실행(FAQ 검색기·에이전트 공통)
- [ ] **1** RAG 체인 직접 조립(`rag_chain`·`answer1`·`chain_with_sources`·`result1` — `{context, question}` → 프롬프트 → 모델 → `StrOutputParser`, `RunnableParallel` 로 근거 함께)
- [ ] **2** Text-to-SQL 주문 건수(`res2`·`tool_msgs2`, 도구 결과 어딘가에 c1=3) + **답까지 스키마로**(`response_format=ProviderStrategy(OrderAnswer, strict=True)` → `res2b`·`report2`) — SQL 준비 셀 실행(sqlite, 준비물 없음)
- [ ] **3** 외부 API 도구 직접 만들기(`jpy_krw_rate`·`used3` — 오늘 환율로 엔화 환산, 채점 셀이 같은 API 로 대조)

## 5. 과제 LV3 (통합) — 리뷰 인텔리전스·조치 리포트 (2문)

- [ ] 준비 셀 실행(모델 공통)
- [ ] **1. 리뷰 인텔리전스 파이프라인** — 1단계 측면 감성 구조화 수집(`batch`·`records`) → 2단계 pandas 집계(`neg_by_aspect`) → 3단계 측면별 교차집계(`summary`)
- [ ] **2. 리뷰 인사이트를 데이터베이스 사실과 잇기** — 1번의 `df` 를 이어 씁니다. 1단계 불만이 가장 많은 도서 찾기(`worst_book`·`worst_count`·`worst_aspect`, 동점이면 이름이 앞선 것 / `worst_count` 는 `int(...)` 로 감쌀 것 — `value_counts()` 는 넘파이 정수를 줍니다) → 2단계 `run_select` 도구를 붙인 `sql_agent` 로 그 책의 제목·가격·재고 조회(`res_sql`·`tool_msgs`) → 3단계 `response_format=ProviderStrategy(ActionReport, strict=True)` 로 조치 리포트 받기(`res_report`·`report`).
  - *채점이 갈리는 지점*: `action` 문장은 모델의 판단이라 존재만 보고, **`stock` 은 도구가 가져온 사실**이라 DB 값과 정확히 대조합니다.

## 부록 (수업에서 다루지 않음)

**완주 기준에 들어가지 않는 참고 자료**입니다. 필요할 때 펴 보세요.

- **`부록_데코레이터_기초.ipynb`** — 교안_01 2절의 **`@tool` 에서 `@` 가 무엇인지** 궁금할 때. 함수도 값이라는
  것부터 시작해 `이름 = 감쌀함수(이름)` 이 `@` 로 줄어드는 과정, `*args`·`**kwargs`, `functools.wraps` 까지
  훑고 `@tool` 을 다시 읽습니다. **모델을 부르지 않아 API 키 없이 돌아갑니다.**

## 막혔을 때

- 준비 셀에서 키 안내와 함께 멈추면 `.env` 에 `OPENAI_API_KEY` 를 채우고 **커널을 재시작**한 뒤 처음부터 실행하세요.
- 검색 결과가 이상하게 많아졌다면 색인 셀을 여러 번 실행한 것입니다 — 색인 셀에 `ids` 가 들어 있으면 몇 번을 실행해도 안전합니다. 그래도 이상하면 커널을 재시작하세요.
- 외부 API 문제(LV2 3번)는 **인터넷 연결**이 필요합니다 — 사내망에서 막히면 다른 망에서 시도하세요.
- 도구가 예상과 다르게 불리면 도구의 **docstring 설명**을 다시 읽어 보세요 — 설명이 라우팅을 좌우합니다(다음 단원에서 증상과 함께 자세히 다룹니다).
- Text-to-SQL 도구가 "에러: ..." 를 돌려주면 모델이 만든 SQL 이 표 구조와 맞지 않은 것입니다. 시스템 프롬프트의 **표 설명**을 고쳐 보세요.
- 같은 코드인데 답이 어제와 다르다면 정상입니다 — 모델은 매번 새로 답합니다. 채점이 통과하는지만 보세요. 단 **도구가 가져온 값과 집계 결과는 늘 같아야** 합니다(LV3 2번의 재고처럼).

## 완주 기준

- 교안 01·02 의 모든 시연·따라하기·퀴즈를 실행/확인했다(부록은 제외).
- 과제 LV1(8문)·LV2(3문)·LV3(2문)의 모든 자가채점이 `✅ 통과!` 다.
- 다음을 스스로 설명할 수 있다 — 구조화된 출력과 도구 호출을 **언제 각각 쓰는지**와 **도구를 쓴 뒤 답까지 스키마로 받는 법**(`response_format`) · 도구의 docstring 이 왜 명세인지 · 에이전트 메시지 궤적을 읽는 법 · LangChain RAG 부품과 LCEL 체인 · Text-to-SQL 의 두 겹 가드 · **비정형에서 뽑은 신호와 정형 데이터의 사실을 잇는 법**과 리포트에서 "모델이 정하는 칸" 과 "도구가 정하는 칸" 을 나누는 이유.
