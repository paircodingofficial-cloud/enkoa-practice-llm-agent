# 이 파일을 .env 로 복사한 뒤 본인 키를 채우세요 (.env 는 공유·커밋 금지).
#
#   cp .env.example .env
#
# 14~16일차에 쓰던 OpenAI 키를 그대로 씁니다. 없으면 준비 셀에서 안내와 함께 멈춥니다.
# 오늘 오가는 요청은 짧은 문장 수십 건이라 비용은 아주 적습니다.

# 발급: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-여기에-본인-키
