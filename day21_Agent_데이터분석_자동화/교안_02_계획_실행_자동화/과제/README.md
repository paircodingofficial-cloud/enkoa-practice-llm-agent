# 교안_02 과제: 계획을 세우고 파이프라인으로 자동화하기

교안_02 에서 본 순서를 문제 네 개로 나눠 직접 만들어 봅니다.
파일마다 `# 여기에 코드를 작성하세요` 자리를 채우고 실행하면 `# [자가채점]` 이 답을 검사합니다.
통과하면 "문제 N 통과" 가 찍히고, 아직 안 푼 문제에서 멈추는 것이 정상입니다.

| 파일 | 무엇 | 모델 호출 |
|---|---|---|
| `문제1_미들웨어_훅_자리.py` | 어느 미들웨어가 루프의 어느 자리를 쓰는지 역방향 표 만들기 | 없음 |
| `문제2_계획_에이전트.py` | TodoListMiddleware 로 복합 요청에 계획을 세우게 하기 | 있음 |
| `문제3_분석_파이프라인.py` | 조회 → 계산 → 그래프 → 리포트를 한 번에 시키기 | 있음 |
| `문제4_결과_정형화.py` | 자유 문장 답을 정해진 틀에 담아 JSON 으로 적재하기 | 있음 |
| `공통.py` | 서버 설정·경로·허용 도구 목록·공통 시스템 프롬프트 | 제공 |
| `정답/` | 문제별 정답 | 강사용 |

문제 1 은 모델도 MCP 서버도 부르지 않아 키 없이 몇 초 만에 끝납니다.

## 실행 방법

이 폴더에서 파일 이름만 주면 됩니다.

```bash
uv run 문제1_미들웨어_훅_자리.py
```

번호 순서대로 푸세요. 문제 2 에서 만든 계획 에이전트를 문제 3 에서 그대로 다시 씁니다.

문제 2~4 는 모델을 부릅니다. 한 문제에 수십 초에서 몇 분이 걸리고, 서버를 처음 받는 첫 실행은 더 걸립니다.
중간에 실패하면 그냥 다시 실행하면 됩니다. 문제 3·4 는 맨 앞에서 지난 산출물을 지우고 시작합니다.

## 산출물이 생기는 곳

문제 3·4 가 만드는 그래프·리포트·JSON 은 모두 이 폴더의 `output/` 에 모입니다.
`공통.py` 가 두 가지를 맞춰 두기 때문입니다.

- 파일시스템 서버에는 **`output/` 폴더**를 허용 폴더로 준다(그 밖은 건드리지 못한다).
- 코드 실행 서버에는 같은 폴더를 **작업 폴더(`cwd`)** 로 준다.

두 서버가 상대경로를 푸는 기준이 원래는 서로 다릅니다. 파일 서버는 **열어 준 폴더**가,
코드 실행 서버는 **작업 폴더**가 기준입니다. 둘을 같은 폴더로 맞춰 두었으므로
에이전트에게는 `장르별_매출.png` 처럼 **파일 이름만** 시키면 됩니다.

## 필요한 것

| 준비물 | 확인 | 없으면 |
|---|---|---|
| `OPENAI_API_KEY` | 일차 폴더의 `.env` | `cp .env.example .env` 후 키 입력 |
| Node.js(`npx`) | `npx -v` | https://nodejs.org (코드 실행·파일시스템 서버) |
| uv(`uvx`) | `uvx --version` | https://docs.astral.sh/uv/ (DB 서버) |
| `data/chinook.db` | 일차 폴더의 `data/` | 배포본의 data 폴더를 그대로 두세요 |
